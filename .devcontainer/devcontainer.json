{
	"name": "Ubuntu",
	"image": "mcr.microsoft.com/devcontainers/base:ubuntu",
	"workspaceFolder": "/workspaces/gemclust",
	"features": {
		"ghcr.io/devcontainers/features/common-utils:2": {
			"installZsh": true,
			"installOhMyZsh": true,
			"username": "automatic",
			"userUid": "automatic",
			"userGid": "automatic",
			"upgradePackages": true
		},
		"ghcr.io/devcontainers/features/kubectl-helm-minikube:1": {}
	},
	"containerUser": "vscode",
	"runArgs": [
		"--security-opt=label=disable"
	],
	"updateRemoteUserUID": true,
	"mounts": [
		{
			"source": "${localEnv:HOME}/.ssh",
			"target": "/home/vscode/.ssh", // Adjust "vscode" to your container's non-root user (e.g., "node", "user")
			"type": "bind",
			"options": "readonly" // Optional: makes keys read-only in container
		},
		{
			"source": "${localEnv:HOME}/talos/kubeconfig",
			"target": "/home/vscode/.kube/config",
			"type": "bind",
			"options": "readonly"
		}
	],
	"postCreateCommand": "./postCreate.sh",
	"postStartCommand": "mkdir -p ~/.kube && cp -a ~/.kube-localhost/. ~/.kube/ 2>/dev/null || true",
	"customizations": {
		"vscode": {
			"settings": {
				"terminal.integrated.defaultProfile.linux": "zsh"
			}
		}
	}
}
