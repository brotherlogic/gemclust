# Apps
This directory is automatically synchronized by Flux as defined in `my-cluster/apps.yaml`.
Add your container workloads and HelmReleases here. Make sure to define a `kustomization.yaml` at the root of this directory and include your manifests.
